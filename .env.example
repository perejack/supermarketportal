# Hashback M-PESA (STK Push)
# Add these in Vercel → Project → Settings → Environment Variables
# For LOCAL testing you can create a `.env.local` with these values.
HASHBACK_API_KEY=
HASHBACK_ACCOUNT_ID=

# Supabase (server)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
